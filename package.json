{
  "name": "@mherwig/my-embeddable-react-app",
  "version": "0.1.0",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "files": [ "dist", "README.md" ],
  "repository": {
    "type": "git",
    "url": "git://github.com/mherwig/my-embeddable-react-app.git"
  },
  "author": "Mike Herwig",
  "keyword": "example,demo,learning",
  "publishConfig": {
    "registry":"https://npm.pkg.github.com/"
  },
  "dependencies": {
    "babel-cli": "^6.26.0",
    "react": "^16.11.0"
  },
  "devDependencies": {
    "react-dom": "^16.11.0",
    "react-scripts": "3.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "rm -rf dist && NODE_ENV=production babel src/lib --out-dir dist --copy-files --ignore __tests__,spec.js,test.js,__snapshots__",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
